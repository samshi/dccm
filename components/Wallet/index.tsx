import s from './index.module.scss'

const Wallet = () => {
  return (
    <img
      src="/images/wallet.png"
      className={s.wallet_logo}
      // onClick={() => router.push('/home')}
    />
  )
}
export default Wallet
